.container
  .page-header
    %nav.btn-group.pull-right
      = view_all_btn(path: polymorphic_path([resource.class]), policy_class: policy_class)

    %h1 New #{resource.model_name}

  = flash_messages
  = render 'form'
