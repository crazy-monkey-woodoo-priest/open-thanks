.container
  .page-header
    %nav.btn-group.pull-right
      = view_all_btn(path: polymorphic_path([resource.class])) if policy.index?

    %h1 New #{resource.model_name}

  = flash_messages
  = render 'form'
